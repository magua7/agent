"""SEC-GO product application layer.

The package initializer intentionally stays dependency-free.  Product adapters
import concrete services from their modules, preserving the Kernel-only install.
"""
